import withAuth from '@/hoc/withAuth';
import React from 'react';

const Lancamentos = () => {
  return (
    <div>
      Informações da Lancamentos
    </div>
  );
};

export default withAuth(Lancamentos);
